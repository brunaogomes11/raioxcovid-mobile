import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
    },
    title:{
        fontSize: 24,
        textAlign: "center",
        padding: 20,
        color: "#000",
    },
    text: {
        fontSize: 18,
        textAlign:"center",
        color: "#000",
    }
})

export default styles